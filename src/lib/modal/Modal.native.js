
import React from 'react';

function Modal({children}) {
    return null;
}

Modal.Container = ({children}) => (

    <React.Fragment>
        {children}
    </React.Fragment>
);


export default Modal;
